shell> mysql -h <host> -u <user> -p < <batch-file>

read in symbol list
for each symbol in list
	have mysql commands here, but plug in symbol
	once symbol plugged into mysql command strings,
	generate a .mysql file
	run it (pass username and pw into this script, forward to mysql script)
	delete .mysql script
next
